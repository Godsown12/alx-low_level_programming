#include <stdio.h>
#include "main.h"
/**
* mul - function that times two numbers
* @a: first number
* @b: second number
* Return: product of a and b
*/
int mul(int a, int b)
{
	product = 0;
	product = a * b;

	return (product);
}
