#include "main.h"
/**
 * _abs - computes the absolute value of a number.
 * @n: This is the variable of the number.
 * Return: (n).
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
