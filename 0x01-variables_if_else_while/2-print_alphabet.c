#include <stdio.h>
/**
 * main - Chaning to capitals letters
 * Return: 0
 */
int main(void)

{

	 /* start the vawulence */

	char a;

	for (alph = 'a'; alph <= 'z'; a++)

		putchar(alph);

	putchar('\n');

	return (0);

}
